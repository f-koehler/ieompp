include_directories(${CMAKE_SOURCE_DIR}/external/catch/include)
include_directories(${CMAKE_SOURCE_DIR}/include)

function(add_hubbard_test TEST_NAME TEST_SOURCE)
    add_executable(${TEST_NAME} "${TEST_SOURCE}")
    if(${hubbard_ENABLE_COVERAGE})
        target_link_libraries(${TEST_NAME} gcov)
        get_target_property(TEMP ${TEST_NAME} COMPILE_FLAGS)
        if(${TEMP} STREQUAL "TEMP-NOTFOUND")
            set(TEMP "")
        endif()
        set(TEMP "${TEMP} -fprofile-arcs -ftest-coverage")
        set_target_properties(${TEST_NAME} PROPERTIES COMPILE_FLAGS ${TEMP})
    endif()
    add_test(${TEST_NAME} ${TEST_NAME})
endfunction()

add_subdirectory(abstract)
add_subdirectory(algebra)
